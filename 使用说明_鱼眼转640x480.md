# 鱼眼视频转 640x480 使用说明

## 1. 输入文件

- 原始鱼眼视频：`video_raw.mp4`
- 标定参数：`rgb_calibration_250801DR48FP25002287_2026-03-08.json`

## 2. 当前转换流程

当前工具不是单纯去畸变，而是按下面这条链路生成右侧结果图：

1. 用 `rotate_anchor_x` 和 `rotate_anchor_y` 决定“先朝哪里看”
2. 在这个去畸变视角上，用 `shift_x` 和 `shift_y` 平移输出窗口
3. 用 `roll_degrees` 对最终结果图再做一次平面内旋转
4. 用 `focal_scale` 控制显示范围大小
5. 输出固定为 `640x480`

当前保存参数文件是：`undistort_params_640x480.json`

## 3. 交互式调参

运行：

```bash
python3 seucm_anchor_gui.py video_raw.mp4 rgb_calibration_250801DR48FP25002287_2026-03-08.json
```

界面说明：

- 左侧：原始鱼眼图像
- 右侧：当前 `640x480` 去畸变预览
- `Frame`：选择预览第几帧
- 点击左图：设置旋转中心 `rotate_anchor_x / rotate_anchor_y`
- `Shift X / Shift Y`：平移右侧显示窗口
- `Roll Degrees`：旋转右侧结果图
- `Focal Scale`：控制视野大小
- `Auto Fit`：自动找一个不越界的显示范围
- `Confirm / Save`：把当前参数保存到 `undistort_params_640x480.json`

左图辅助标记：

- 红色十字：旋转中心
- 绿色轮廓：右侧当前预览对应的原图范围

## 4. 按保存参数导出视频

运行：

```bash
python3 render_video_seucm_params.py \
  video_raw.mp4 \
  rgb_calibration_250801DR48FP25002287_2026-03-08.json \
  undistort_params_640x480.json \
  video_undistorted_640x480.mp4
```

输出文件：

- `video_undistorted_640x480.mp4`

## 5. 当前这一版实际使用的参数

当前参数文件内容如下：

```json
{
  "rotate_anchor_x": 526.0,
  "rotate_anchor_y": 845.0,
  "shift_x": 40.0,
  "shift_y": 53.0,
  "roll_degrees": -7.9,
  "focal_scale": 0.833,
  "output_width": 640,
  "output_height": 480
}
```

## 6. 一句话理解

如果要从原始鱼眼视频得到当前效果，最简单的做法就是：

1. 打开 GUI
2. 选帧观察
3. 点击左图确定朝向
4. 调 `shift / roll / focal`
5. 保存参数
6. 用导出脚本生成最终视频
